version: '3.8'
services:
  cloudops-app:
    build: .
    ports:
      - "80:80"
    container_name: cloudops-automator
    restart: always
    volumes:
      - ./html:/usr/share/nginx/html
    networks:
      - cloudops-net

networks:
  cloudops-net:
    driver: bridge
